# Current Task

**Phase 6: Homepage build** — In progress, following `docs/homepage-plan.md` §2.8.

## Current step
**§2.8 step 6 — add homepage CSS.** Add `.hero`, `.hero-cta`, `.btn-secondary`,
`.feature-grid`, `.feature-card`, `.stat-grid`, `.stat`, `.stat-num`,
`.stat-label` to `static/css/main.css`. All colors must reuse existing
custom properties (`--accent`, `--surface`, `--border`, `--text`,
`--text-secondary`, `--radius`); no new tokens.

## What's next
§2.8 step 7: pre-commit run --all-files. Step 8: confirm 102 tests still green.
Step 9: tracking-file updates + final verification checklist.

## Branch
`feature/policyiq-homepage` (created from `main`).
