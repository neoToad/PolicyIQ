# Current Task

**Phase 6: Homepage build** — In progress, following `docs/homepage-plan.md` §2.8.

## Current step
**§2.8 step 7 — pre-commit run --all-files.** Need to install hooks first
(per Makefile: `make pre-commit-install`), then run them across the whole
repo. Should be clean for all hooks (trailing-whitespace, EOF, ruff).

## What's next
§2.8 step 8: full test suite green at 102 (already true; will re-verify).
Step 9: CHANGELOG entry for the build + final CURRENT_TASK.md reset.

## Branch
`feature/policyiq-homepage` (created from `main`).
